//2024.4.29 第一节课，实现了，从头文件里面的窗口函数里面公共的接口里面声明一个公共的父有属性，即在对应的子类操作中，就不需要局部变量传递了

//在关闭这个功能的实现的过程中，就是直接调用其爷爷里面的isopen(),然后再清楚当前显示的文本
//这个file.close()只是将当前的打开的这个file在内存中给释放了，并不会把内容也同步释放，内容的释放，是需要调用其他的api的

//2024.4.29.上午学习了QT的四结课，第一个是学习了记事本实现关闭按键

//第二节，实现了字符编码问题的引入，但，我的操作系统，编译环境的设置跟老师的设置应该是不一样的，我这里，不管怎么设置，utf-8和ansi的编码都是能相互转换的所以，
//在我的环境下按照老师的操作,并没有出现对应的问题

//第三节，了解了ComBox组件的设置
//设置完ComBox之后，再到头文件这个wight这个大对话框的类，里面声明ComBox这个按钮的函数，再到wight.cpp里面去实现，首先实现的槽函数与信号函数的绑定，其实是具体函数的实现
//

//====但是我对于槽函数与信号函数具体的底层细节还是不怎么清楚=======


//第四节，在记事本项目中，实现了combox和编码的组合问题的解决，但是我在具体还是的实现方面还是有问题

//今天下午，解决疑问
